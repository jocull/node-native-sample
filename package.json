{
  "name": "native-addon",
  "version": "0.0.1",
  "description": "A sample native C++ module that will execute intense code asynchronously on the worker queue",
  "repository": "https://github.com/jocull/node-native-sample",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node-gyp rebuild",
    "start": "node-gyp rebuild && node index.js"
  },
  "author": "James O'Cull",
  "license": "MIT",
  "gypfile": true,
  "dependencies": {
    "nan": "^2.3.5"
  }
}
